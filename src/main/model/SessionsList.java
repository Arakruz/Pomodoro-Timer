package model;

import java.util.ArrayList;

// Represents a list of all sessions
public class SessionsList {
    ArrayList<FocusSession> sessionsArray;

    public SessionsList() {
        sessionsArray = new ArrayList<FocusSession>();
    }
}
